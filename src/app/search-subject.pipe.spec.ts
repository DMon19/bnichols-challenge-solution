import { SearchSubjectPipe } from './search-subject.pipe';

describe('SearchSubjectPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
